<% @matches = @week.matches %>

<% if @matches.any? %>
    <!-- display matches grouped by game# -->
    <%= render @matches %>
    <br/>
    <%= link_to 'PDF Schedules', week_path(@week, format: 'pdf') %> |
<% else %>
    No matches
<% end %>