<% provide(:title, "Welcome to #{League::NAME}") %>

<h1>Welcome to <%= League::NAME %></h1>

<% if @leagues and @leagues.any? %>

    <%= render 'team_search' %>

    <ul class="rounded-list">
      <%= render @leagues %>
    </ul>

<% else %>
      No open events. Try again later.
<% end %>

<br/>

<%= link_to 'Add event', new_league_path if admin? %>
